<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuzj.mapper.UserLogMapper">

    <insert id="insert">
        insert into user_log(user_id, username, type, pre_log_id, question, answer, date_time, consume_time)
            value (#{userId}, #{username}, #{type}, #{preLogId}, #{question}, #{answer}, #{dateTime}, #{consumeTime})
    </insert>
</mapper>